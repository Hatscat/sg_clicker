function render () {

	switch (current_scene) {
		case SC_GAME:
			draw_game_sc(t);
		break;
		case SC_GAME:
			draw_game_sc(t);
		break;
	}

	real_ctx.drawImage(main_buffer, 0, 0);
}

function draw_game_sc () {

}